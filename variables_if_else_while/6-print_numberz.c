#include <stdio.h>

/**
 * main - Entry point
 * Description: 'print all digits of base 10 in order'
 * Return: Always 0 (Success)
 */

int main(void)
{
        int x;

        for (x = 0; x <= 9; x++)
        {
        int putchar(x);
        }
        putchar('\n');
        return (0);
}
