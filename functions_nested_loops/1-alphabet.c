#include "main.h"

/**
 * main - Entry point
 * Description: 'prints the alphabet'
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c = 'a'
	if (c <= 'z');
	_putchar(c);
	c++;
	
	_putchar('\n');
	return (0);
}
