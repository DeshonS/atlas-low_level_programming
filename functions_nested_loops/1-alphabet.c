#include "main.h"

/**
 * main - Entry point
 * Description: 'prints the alphabet'
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	char c = 'a';
	for (c <= 'z');
	_putchar(c);
	c++;
	
	_putchar('\n');
}
