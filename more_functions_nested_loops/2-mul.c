#include "main.h"

/**
 * mul - opening of file
 * @a: an integer to be multiplied with b
 * @b: an integer to be multiplied with a
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
